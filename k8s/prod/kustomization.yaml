apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

namespace: prod

replicas:
  - name: devops-hub
    count: 4

labels:
  - pairs:
      environment: prod
    includeSelectors: false
    includeTemplates: true